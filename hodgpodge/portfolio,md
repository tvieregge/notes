postgres
 creating cluster
 sudo -u postgres initdb --locale en_US.UTF-8 -D '/var/lib/postgres/data' --data-checksums                                                                                                                          src/recurse-community-portfolio master
 postgres user by default superuser

 sudo systemctl start postgresql                                                                                                                                                                                    src/recurse-community-portfolio master
 can rely on unix to do auth

 sudo -u postgres createdb --owner=tim tim                                                                                                                                                                          src/recurse-community-portfolio master

 client version >= server version
 psql -h localhost -U portfolio


\d obj in DB
\x auto Expanded display is used automatically
flyway - migration mgr
thymeleaf - template engine

### Add tag desc
description of tag
editable
